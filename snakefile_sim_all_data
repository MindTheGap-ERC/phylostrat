NCHAR = [ "30", "100", "300", "1000"]
NO_OF_REPLICAS = 50
ID = [str(i) for i in range(1, NO_OF_REPLICAS + 1)]
CASES = ["sinusoid", "miller"]
STRAT_CASE = ["sinusoid"]
ANALYSIS_CASE = ["gap_est", "gap_prior"]
BASE_COLLECTION_SIM = "irods://nluu11p/home/research-mindthegap/phylostrat/sim_data/"

storage yoda:
    provider="irods",
    max_requests_per_second="1000"

rule all:
    input:
        char_mat_base = storage.yoda( expand( BASE_COLLECTION_SIM + "fbd_base/char_mat_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR)),
        fossils_base = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_base/fossils_{id}_nchar{nchar}.csv", id = ID, nchar = NCHAR)),
        tree_base = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_base/tree_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR)),
        mol_base = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_base/mol_dat_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR)),
        or_tree_base = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_base/original_tree_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR)),
        or_fossils_base = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_base/original_fossils_{id}_nchar{nchar}.csv", id = ID, nchar = NCHAR)), 
        char_mat_strat = storage.yoda( expand( BASE_COLLECTION_SIM + "fbd_strat/char_mat_{id}_nchar{nchar}_{case}.nex", id = ID, nchar = NCHAR, case = CASES)),
        fossils_strat = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_strat/fossils_{id}_nchar{nchar}_{case}.csv", id = ID, nchar = NCHAR, case = CASES)),
        tree_strat = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_strat/tree_{id}_nchar{nchar}_{case}.nex", id = ID, nchar = NCHAR, case = CASES)),
        mol_strat = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_strat/mol_dat_{id}_nchar{nchar}_{case}.nex", id = ID, nchar = NCHAR, case = CASES)),
        or_tree_strat = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_strat/original_tree_{id}_nchar{nchar}_{case}.nex", id = ID, nchar = NCHAR, case = CASES)),
        or_fossils_strat = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_strat/original_fossils_{id}_nchar{nchar}_{case}.csv", id = ID, nchar = NCHAR, case = CASES)), 
        char_mat_test = storage.yoda( expand( BASE_COLLECTION_SIM + "fbd_test/char_mat_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR)),
        fossils_test = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_test/fossils_{id}_nchar{nchar}.csv", id = ID, nchar = NCHAR)),
        tree_test = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_test/tree_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR)),
        mol_test = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_test/mol_dat_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR)),
        or_tree_test = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_test/original_tree_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR)),
        or_fossils_test = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_test/original_fossils_{id}_nchar{nchar}.csv", id = ID, nchar = NCHAR)), 
        char_mat_sl = storage.yoda( expand( BASE_COLLECTION_SIM + "fbd_{analysis_case}/char_mat_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE, analysis_case = ANALYSIS_CASE)),
        fossils_sl = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_{analysis_case}/fossils_{id}_nchar{nchar}_{strat_case}.csv", id = ID, nchar = NCHAR, strat_case = STRAT_CASE, analysis_case = ANALYSIS_CASE)),
        tree_sl = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_{analysis_case}/tree_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE, analysis_case = ANALYSIS_CASE)),
        mol_sl = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_{analysis_case}/mol_dat_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE, analysis_case = ANALYSIS_CASE)),
        or_tree_sl = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_{analysis_case}/original_tree_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE, analysis_case = ANALYSIS_CASE)),
        or_fossils_sl = storage.yoda(expand(BASE_COLLECTION_SIM + "fbd_{analysis_case}/original_fossils_{id}_nchar{nchar}_{strat_case}.csv", id = ID, nchar = NCHAR, strat_case = STRAT_CASE, analysis_case = ANALYSIS_CASE)) 
        
rule simulate_data_locally_fbd_base:
    output:
        trees = expand("sim_data/fbd_base/tree_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR),
        char_mat = expand("sim_data/fbd_base/char_mat_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR),
        fossils = expand("sim_data/fbd_base/fossils_{id}_nchar{nchar}.csv", id = ID, nchar = NCHAR),
        mol = expand("sim_data/fbd_base/mol_dat_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR),
        or_trees = expand("sim_data/fbd_base/original_tree_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR),
        or_fossils = expand("sim_data/fbd_base/original_fossils_{id}_nchar{nchar}.csv", id = ID, nchar = NCHAR),
    shell:
        "Rscript code/sim_base.R"

rule simulate_data_locally_fbd_gap_est:
    output:
        trees = expand("sim_data/fbd_gap_est/tree_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        char_mat = expand("sim_data/fbd_gap_est/char_mat_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        fossils = expand("sim_data/fbd_gap_est/fossils_{id}_nchar{nchar}_{strat_case}.csv", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        mol = expand("sim_data/fbd_gap_est/mol_dat_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        or_tree = expand("sim_data/fbd_gap_est/original_tree_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        or_fossils = expand("sim_data/fbd_gap_est/original_fossils_{id}_nchar{nchar}_{strat_case}.csv", id = ID, nchar = NCHAR, strat_case = STRAT_CASE)
    shell:
        "Rscript code/sim_gap_est.R"

rule simulate_data_locally_fbd_gap_prior:
    output:
        trees = expand("sim_data/fbd_gap_prior/tree_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        char_mat = expand("sim_data/fbd_gap_prior/char_mat_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        fossils = expand("sim_data/fbd_gap_prior/fossils_{id}_nchar{nchar}_{strat_case}.csv", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        mol = expand("sim_data/fbd_gap_prior/mol_dat_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        or_tree = expand("sim_data/fbd_gap_prior/original_tree_{id}_nchar{nchar}_{strat_case}.nex", id = ID, nchar = NCHAR, strat_case = STRAT_CASE),
        or_fossils = expand("sim_data/fbd_gap_prior/original_fossils_{id}_nchar{nchar}_{strat_case}.csv", id = ID, nchar = NCHAR, strat_case = STRAT_CASE)
    shell:
        "Rscript code/sim_gap_prior.R"

rule simulate_data_locally_fbd_strat:
    output:
        trees = expand("sim_data/fbd_strat/tree_{id}_nchar{nchar}_{case}.nex", id = ID, nchar = NCHAR, case = CASES),
        char_mat = expand("sim_data/fbd_strat/char_mat_{id}_nchar{nchar}_{case}.nex", id = ID, nchar = NCHAR, case = CASES),
        fossils = expand("sim_data/fbd_strat/fossils_{id}_nchar{nchar}_{case}.csv", id = ID, nchar = NCHAR, case = CASES),
        mol = expand("sim_data/fbd_strat/mol_dat_{id}_nchar{nchar}_{case}.nex", id = ID, nchar = NCHAR, case = CASES),
        or_tree = expand("sim_data/fbd_strat/original_tree_{id}_nchar{nchar}_{case}.nex", id = ID, nchar = NCHAR, case = CASES),
        or_fossils = expand("sim_data/fbd_strat/original_fossils_{id}_nchar{nchar}_{case}.csv", id = ID, nchar = NCHAR, case = CASES)
    shell:
        "Rscript code/sim_strat.R"

rule simulate_data_locally_fbd_test:
    output:
        trees = expand("sim_data/fbd_test/tree_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR),
        char_mat = expand("sim_data/fbd_test/char_mat_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR),
        fossils = expand("sim_data/fbd_test/fossils_{id}_nchar{nchar}.csv", id = ID, nchar = NCHAR),
        mol = expand("sim_data/fbd_test/mol_dat_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR),
        or_trees = expand("sim_data/fbd_test/original_tree_{id}_nchar{nchar}.nex", id = ID, nchar = NCHAR),
        or_fossils = expand("sim_data/fbd_test/original_fossils_{id}_nchar{nchar}.csv", id = ID, nchar = NCHAR),
    shell:
        "Rscript code/sim_test.R"

rule copy_data_to_yoda_base:
    input:
        char_mat = "sim_data/fbd_base/char_mat_{id}_nchar{nchar}.nex",
        fossils = "sim_data/fbd_base/fossils_{id}_nchar{nchar}.csv",
        tree = "sim_data/fbd_base/tree_{id}_nchar{nchar}.nex",
        mol = "sim_data/fbd_base/mol_dat_{id}_nchar{nchar}.nex",
        or_tree = "sim_data/fbd_base/original_tree_{id}_nchar{nchar}.nex",
        or_fossils = "sim_data/fbd_base/original_fossils_{id}_nchar{nchar}.csv"
    output:
        char_mat = storage.yoda(BASE_COLLECTION_SIM + "fbd_base/char_mat_{id}_nchar{nchar}.nex"),
        fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_base/fossils_{id}_nchar{nchar}.csv"),
        tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_base/tree_{id}_nchar{nchar}.nex"),
        mol = storage.yoda(BASE_COLLECTION_SIM + "fbd_base/mol_dat_{id}_nchar{nchar}.nex"),
        or_tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_base/original_tree_{id}_nchar{nchar}.nex"),
        or_fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_base/original_fossils_{id}_nchar{nchar}.csv")
    shell:
        """
        cp {input.char_mat} {output.char_mat}
        cp {input.fossils} {output.fossils}
        cp {input.tree} {output.tree}
        cp {input.mol} {output.mol}
        cp {input.or_tree} {output.or_tree}
        cp {input.or_fossils} {output.or_fossils}
        """


rule copy_data_to_yoda_strat:
    input:
        char_mat = "sim_data/fbd_strat/char_mat_{id}_nchar{nchar}_{case}.nex",
        fossils = "sim_data/fbd_strat/fossils_{id}_nchar{nchar}_{case}.csv",
        tree = "sim_data/fbd_strat/tree_{id}_nchar{nchar}_{case}.nex",
        mol = "sim_data/fbd_strat/mol_dat_{id}_nchar{nchar}_{case}.nex",
        or_tree = "sim_data/fbd_strat/original_tree_{id}_nchar{nchar}_{case}.nex",
        or_fossils = "sim_data/fbd_strat/original_fossils_{id}_nchar{nchar}_{case}.csv"
    output:
        char_mat = storage.yoda(BASE_COLLECTION_SIM + "fbd_strat/char_mat_{id}_nchar{nchar}_{case}.nex"),
        fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_strat/fossils_{id}_nchar{nchar}_{case}.csv"),
        tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_strat/tree_{id}_nchar{nchar}_{case}.nex"),
        mol = storage.yoda(BASE_COLLECTION_SIM + "fbd_strat/mol_dat_{id}_nchar{nchar}_{case}.nex"),
        or_tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_strat/original_tree_{id}_nchar{nchar}_{case}.nex"),
        or_fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_strat/original_fossils_{id}_nchar{nchar}_{case}.csv")
    shell:
        """
        cp {input.char_mat} {output.char_mat}
        cp {input.fossils} {output.fossils}
        cp {input.tree} {output.tree}
        cp {input.mol} {output.mol}
        cp {input.or_tree} {output.or_tree}
        cp {input.or_fossils} {output.or_fossils}
        """


rule copy_data_to_yoda_test:
    input:
        char_mat = "sim_data/fbd_test/char_mat_{id}_nchar{nchar}.nex",
        fossils = "sim_data/fbd_test/fossils_{id}_nchar{nchar}.csv",
        tree = "sim_data/fbd_test/tree_{id}_nchar{nchar}.nex",
        mol = "sim_data/fbd_test/mol_dat_{id}_nchar{nchar}.nex",
        or_tree = "sim_data/fbd_test/original_tree_{id}_nchar{nchar}.nex",
        or_fossils = "sim_data/fbd_test/original_fossils_{id}_nchar{nchar}.csv"
    output:
        char_mat = storage.yoda(BASE_COLLECTION_SIM + "fbd_test/char_mat_{id}_nchar{nchar}.nex"),
        fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_test/fossils_{id}_nchar{nchar}.csv"),
        tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_test/tree_{id}_nchar{nchar}.nex"),
        mol = storage.yoda(BASE_COLLECTION_SIM + "fbd_test/mol_dat_{id}_nchar{nchar}.nex"),
        or_tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_test/original_tree_{id}_nchar{nchar}.nex"),
        or_fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_test/original_fossils_{id}_nchar{nchar}.csv")
    shell:
        """
        cp {input.char_mat} {output.char_mat}
        cp {input.fossils} {output.fossils}
        cp {input.tree} {output.tree}
        cp {input.mol} {output.mol}
        cp {input.or_tree} {output.or_tree}
        cp {input.or_fossils} {output.or_fossils}
        """

rule copy_data_to_yoda_gap_prior:
    input:
        char_mat = "sim_data/fbd_gap_prior/char_mat_{id}_nchar{nchar}_{strat_case}.nex",
        fossils = "sim_data/fbd_gap_prior/fossils_{id}_nchar{nchar}_{strat_case}.csv",
        tree = "sim_data/fbd_gap_prior/tree_{id}_nchar{nchar}_{strat_case}.nex",
        mol = "sim_data/fbd_gap_prior/mol_dat_{id}_nchar{nchar}_{strat_case}.nex",
        or_tree = "sim_data/fbd_gap_prior/original_tree_{id}_nchar{nchar}_{strat_case}.nex",
        or_fossils = "sim_data/fbd_gap_prior/original_fossils_{id}_nchar{nchar}_{strat_case}.csv"
    output:
        char_mat = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_prior/char_mat_{id}_nchar{nchar}_{strat_case}.nex"),
        fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_prior/fossils_{id}_nchar{nchar}_{strat_case}.csv"),
        tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_prior/tree_{id}_nchar{nchar}_{strat_case}.nex"),
        mol = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_prior/mol_dat_{id}_nchar{nchar}_{strat_case}.nex"),
        or_tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_prior/original_tree_{id}_nchar{nchar}_{strat_case}.nex"),
        or_fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_prior/original_fossils_{id}_nchar{nchar}_{strat_case}.csv")
    shell:
        """
        cp {input.char_mat} {output.char_mat}
        cp {input.fossils} {output.fossils}
        cp {input.tree} {output.tree}
        cp {input.mol} {output.mol}
        cp {input.or_tree} {output.or_tree}
        cp {input.or_fossils} {output.or_fossils}
        """


rule copy_data_to_yoda_gap_est:
    input:
        char_mat = "sim_data/fbd_gap_est/char_mat_{id}_nchar{nchar}_{strat_case}.nex",
        fossils = "sim_data/fbd_gap_est/fossils_{id}_nchar{nchar}_{strat_case}.csv",
        tree = "sim_data/fbd_gap_est/tree_{id}_nchar{nchar}_{strat_case}.nex",
        mol = "sim_data/fbd_gap_est/mol_dat_{id}_nchar{nchar}_{strat_case}.nex",
        or_tree = "sim_data/fbd_gap_est/original_tree_{id}_nchar{nchar}_{strat_case}.nex",
        or_fossils = "sim_data/fbd_gap_est/original_fossils_{id}_nchar{nchar}_{strat_case}.csv"
    output:
        char_mat = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_est/char_mat_{id}_nchar{nchar}_{strat_case}.nex"),
        fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_est/fossils_{id}_nchar{nchar}_{strat_case}.csv"),
        tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_est/tree_{id}_nchar{nchar}_{strat_case}.nex"),
        mol = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_est/mol_dat_{id}_nchar{nchar}_{strat_case}.nex"),
        or_tree = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_est/original_tree_{id}_nchar{nchar}_{strat_case}.nex"),
        or_fossils = storage.yoda(BASE_COLLECTION_SIM + "fbd_gap_est/original_fossils_{id}_nchar{nchar}_{strat_case}.csv")
    shell:
        """
        cp {input.char_mat} {output.char_mat}
        cp {input.fossils} {output.fossils}
        cp {input.tree} {output.tree}
        cp {input.mol} {output.mol}
        cp {input.or_tree} {output.or_tree}
        cp {input.or_fossils} {output.or_fossils}
        """

